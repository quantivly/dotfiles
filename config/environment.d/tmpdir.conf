# Keeps TMPDIR off the /tmp tmpfs (RAM-backed) for processes started by the
# graphical session, which never reads a shell rc. environment.d(5) supports
# only $VAR/${VAR} shell-style expansion, not %h — that's a unit-file
# specifier and is passed through literally here.
TMPDIR=${HOME}/.cache/tmp
