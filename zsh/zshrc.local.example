# Machine-specific zsh configuration
# Copy this file to ~/.zshrc.local and customize for each machine
# This file should NEVER be committed to git!

#==============================================================================
# Secrets and API keys
#==============================================================================

# Docker Hub credentials
# export DOCKERHUB_USERNAME=your-username
# export DOCKERHUB_TOKEN=your-token

# Linear API key
# export LINEAR_API_KEY=your-key

# GitHub tokens (if needed beyond gh CLI)
# export GITHUB_TOKEN=your-token

# Other API keys
# export OPENAI_API_KEY=your-key
# export AWS_ACCESS_KEY_ID=your-key
# export AWS_SECRET_ACCESS_KEY=your-secret

#==============================================================================
# SSH Configuration
#==============================================================================

# Auto-add SSH keys on shell startup
# SSH_KEY_PATH="${HOME}/.ssh/id_ed25519"
# if [ -f "$SSH_KEY_PATH" ]; then
#     ssh-add -l | grep -q "$SSH_KEY_PATH" || ssh-add "$SSH_KEY_PATH" 2>/dev/null
# fi

#==============================================================================
# Machine-specific PATH additions
#==============================================================================

# Example: Add custom tool directories
# pathadd "${HOME}/dcm4che-5.29.2/bin"
# pathadd "${HOME}/go/bin"
# pathadd "${HOME}/bin"
# pathadd "/opt/homebrew/bin"  # macOS Apple Silicon

#==============================================================================
# Work/Project-specific overrides
#==============================================================================

# Override Quantivly settings for this machine
# export Q_MODE="production"
# export Q_DEV_CODE_ROOT="${HOME}/projects/quantivly"
# export Q_WORKSPACE_NAME="my-workspace"

#==============================================================================
# Machine-specific aliases
#==============================================================================

# Example: Shortcuts to frequently used directories
# alias proj='cd ~/projects'
# alias work='cd ~/work'

# Example: Machine-specific Docker or SSH shortcuts
# alias ssh-prod='ssh user@production.example.com'
# alias docker-dev='docker exec -it dev-container bash'

#==============================================================================
# Performance tuning
#==============================================================================

# Disable oh-my-zsh plugins that aren't needed on this machine
# This can speed up shell startup significantly
# Example: If you don't use poetry on a particular machine:
# plugins=(${plugins:#poetry})

#==============================================================================
# Other machine-specific settings
#==============================================================================

# Set custom prompt elements (if needed)
# export POWERLEVEL9K_LEFT_PROMPT_ELEMENTS=(context dir vcs)

# Machine-specific locale (if different from default)
# export LANG=en_GB.UTF-8
# export LC_ALL=en_GB.UTF-8
